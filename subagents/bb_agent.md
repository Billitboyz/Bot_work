# bb_agent Documentation

This sub-agent responds to the command `/bb`.

When `/bb` is received, it sends a message starting with `BILLIT` followed by a blank line, then the message body.

Example response:
```
BILLIT

Response to /bb command
```
